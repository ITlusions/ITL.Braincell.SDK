name: CI - Test and Validate

on:
  pull_request:
    branches: [ main, develop ]
  push:
    branches: [ main, develop, 'release/**', 'feature/**', 'hotfix/**' ]
  workflow_dispatch:

jobs:
  ci:
    uses: ./.github/workflows/ci-reusable.yml
    with:
      python_version: '3.12'
      test_import_path: 'itl_braincell_sdk.core.config'
      test_import_class: 'Settings'
      src_directory: 'src'
